<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<entry key="selectGame">
		SELECT GAME_NO
		     , COMPANY_NO
		     , KOR_NAME
		     , ENG_NAME
		     , TO_CHAR(RELEASE_DATE, '""YYYY"년 "MM"월 "DD"일"') "RELEASE_DATE"
		     , PRICE
		     , POINT
		     , GAME_STATUS
		     , GAME_IMG 
		  FROM GAME
		 WHERE KOR_NAME || ENG_NAME LIKE '%' || ? || '%'
	</entry>

</properties>